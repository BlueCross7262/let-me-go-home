# Agent instructions for this repository

Read `CLAUDE.md`. It holds the repository instructions — layout, invariants,
build and test commands, and how to measure the shipped surface — and this file
does not restate them.

`AGENTS.md` exists so agents that look for this filename by convention find the
same instructions instead of nothing.
